var assert  = require('assert');
var diez    = require('..');

describe('.create', function() {
  it('should create a new instance', function() {
    var f = function() {
      return { 'foo': 'bar' };
    };
  });
});
